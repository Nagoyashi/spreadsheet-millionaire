# v0.7.0 — Numeric input hardening (2026-06-13)

- Bounded + clamped numeric inputs across the four published calculators.
- `fmt()` gained B/T tiers and a `$999T+` display ceiling; added a `finiteOr`
  guard for derived metrics (no more `Infinity`/`NaN` in ratios).

→ `project.md` Phase log · `DECISIONS.md` § "Numeric input is bounded and clamped at the shared component".
