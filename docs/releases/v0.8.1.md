# v0.8.1 — Post-launch patch (2026-06-14)

First post-launch build-in-public patch — no new phase.

- **httpClient hardened** against network failures, timeouts, and non-JSON
  responses, so auth/reset forms no longer freeze on a Render cold-start 502
  (30s `AbortController` timeout, uniform error shape, shared `describeError`) (#17).
- **Legal pages finalized:** real operator details (Impressum), German governing
  law (Terms), real published contact email; pre-launch placeholder scaffolding
  removed.
- **Phase 8 launch recorded** in `project.md` / `STATUS.md`.

→ `project.md` Phase log.
